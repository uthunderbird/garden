.DS_Store

# Черновики писателя и бэкапы — не должны попадать в паблик.
# Защита была случайной: файлы просто никогда не коммитили. Один
# `git add -A` опубликовал бы их все — Quartz их тоже не исключает.
*.writer.md
*.backup-*.md
*.backup.md
